The pyautogui library is used.
What it does: copies the last edited file from one directory to another,
while saving the current system time in the file name.
It makes it easier to collect client logs, remembers the paths where the logs are and where to copy them.